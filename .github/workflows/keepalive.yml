name: Keep LeoCore Backend Awake

on:
  schedule:
    # Runs every 10 minutes
    - cron: "*/10 * * * *"
  workflow_dispatch:

jobs:
  keepalive:
    runs-on: ubuntu-latest

    steps:
      - name: Ping backend
        run: |
          curl -s https://leocore.onrender.com/ping > /dev/null

